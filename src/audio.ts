export class Sound {
 enabled=true;ctx:AudioContext|null=null;lastCollect=0;
 unlock(){if(!this.ctx)this.ctx=new AudioContext();void this.ctx.resume();}
 tone(freq:number,duration:number,volume=.04,type:OscillatorType='sine',end=freq){if(!this.enabled||!this.ctx)return;const c=this.ctx,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(freq,c.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(20,end),c.currentTime+duration);g.gain.setValueAtTime(volume,c.currentTime);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+duration);o.connect(g).connect(c.destination);o.start();o.stop(c.currentTime+duration);}
 play(type:string){if(type==='hit'){this.tone(100,.19,.08,'triangle',35);this.tone(420,.035,.025,'square',120);}else if(type==='break'||type==='impact')this.tone(140+Math.random()*100,.13,.025,'triangle',45);else if(type==='swing')this.tone(240,.11,.02,'sine',85);else if(type==='danger'){this.tone(500,.16,.055,'triangle',270);}else if(type==='collect'){const now=performance.now();if(now-this.lastCollect>90){this.lastCollect=now;this.tone(700+Math.random()*400,.13,.018);}}else if(type==='win')[523,659,784,1046].forEach((f,i)=>setTimeout(()=>this.tone(f,.5,.045),i*100));else if(type==='fail')this.tone(260,.45,.05,'triangle',90);else if(type==='upgrade'){this.tone(400,.2,.04,'sine',900);}else this.tone(520,.08,.025);}
}
